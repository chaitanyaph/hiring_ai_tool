package com.cadence.codingassessmentservice.constants;

/** Matches the Figma wizard's chip picker and the IDE's language selector exactly -- no more, no fewer. */
public enum ProgrammingLanguage {
    JAVA,
    PYTHON,
    JAVASCRIPT,
    CPP,
    SQL
}
