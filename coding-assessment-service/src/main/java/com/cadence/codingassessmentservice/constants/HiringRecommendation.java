package com.cadence.codingassessmentservice.constants;

/** Mirrors ai-interview-service's own enum exactly -- this is the value carried on the CandidateRecommended event this service consumes. */
public enum HiringRecommendation {
    PROCEED,
    HOLD,
    REJECT
}
