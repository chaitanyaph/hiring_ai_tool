spring:
  datasource:
    url: jdbc:mysql://codingassessment-db:3306/coding_assessment_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
  data:
    redis:
      host: redis
  kafka:
    bootstrap-servers: kafka:9092
coding-assessment:
  execution:
    judge0:
      base-url: http://judge0-server:2358
eureka:
  client:
    enabled: true
    service-url:
      defaultZone: http://eureka-server:8761/eureka
