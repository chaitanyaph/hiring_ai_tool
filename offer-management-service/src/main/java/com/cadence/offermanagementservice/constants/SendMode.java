package com.cadence.offermanagementservice.constants;

/** Matches the Figma wizard's step-4 radio-card exactly: Save as draft / Send for approval / Send to candidate. */
public enum SendMode {
    DRAFT,
    APPROVAL,
    SEND
}
