spring:
  datasource:
    url: jdbc:mysql://resume-db:3306/resume_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
  data:
    redis:
      host: redis
  kafka:
    bootstrap-servers: kafka:9092
app:
  minio:
    endpoint: http://minio:9000
eureka:
  client:
    enabled: true
    service-url:
      defaultZone: http://eureka-server:8761/eureka
