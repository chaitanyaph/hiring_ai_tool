spring:
  datasource:
    url: jdbc:mysql://interviewmanagement-db:3306/interview_management_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
  data:
    redis:
      host: redis
  kafka:
    bootstrap-servers: kafka:9092
eureka:
  client:
    enabled: true
    service-url:
      defaultZone: http://eureka-server:8761/eureka
