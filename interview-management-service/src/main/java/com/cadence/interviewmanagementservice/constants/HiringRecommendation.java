package com.cadence.interviewmanagementservice.constants;

/** Mirrors ai-interview-service's own enum exactly -- the value carried on the CandidateRecommended event this service consumes. */
public enum HiringRecommendation {
    PROCEED,
    HOLD,
    REJECT
}
