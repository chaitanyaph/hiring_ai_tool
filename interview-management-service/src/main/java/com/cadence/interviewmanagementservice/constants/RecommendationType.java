package com.cadence.interviewmanagementservice.constants;

/** Matches the Figma's submit-feedback modal exactly (Proceed/Hold/Reject) -- not the text spec's HIRE/STRONG_HIRE/REJECT/HOLD set. */
public enum RecommendationType {
    PROCEED,
    HOLD,
    REJECT
}
