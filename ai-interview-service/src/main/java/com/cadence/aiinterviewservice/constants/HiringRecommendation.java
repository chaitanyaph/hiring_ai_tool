package com.cadence.aiinterviewservice.constants;

/** Same three-value vocabulary Resume Parser Service's AI recommendation already uses -- kept consistent across every AI-recommendation surface rather than inventing a second scale. */
public enum HiringRecommendation {
    PROCEED,
    HOLD,
    REJECT
}
