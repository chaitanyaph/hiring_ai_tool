package com.cadence.resumeparserservice.constants;

/** Same three-value vocabulary the platform's AI Interview Evaluation already uses -- kept consistent across every AI-recommendation surface rather than inventing a second scale. */
public enum HiringRecommendation {
    PROCEED,
    HOLD,
    REJECT
}
