package com.cadence.applicationservice.constant;

/** Mirrors ai-interview-service's own HiringRecommendation enum exactly -- carried on the InterviewEvaluatedEvent this service consumes. */
public enum HiringRecommendation {
    PROCEED,
    HOLD,
    REJECT
}
