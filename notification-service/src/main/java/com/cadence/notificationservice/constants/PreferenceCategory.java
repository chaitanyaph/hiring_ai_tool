package com.cadence.notificationservice.constants;

/** Matches the Figma's candidate Settings -> Notifications tab exactly (4 categories, single combined email+in-app switch each). */
public enum PreferenceCategory {
    APPLICATION_STATUS_UPDATES,
    INTERVIEW_REMINDERS,
    RECOMMENDED_JOBS,
    MARKETING_EMAILS
}
